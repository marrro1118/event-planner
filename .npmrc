# ~/.npmrc は script-shell に Git の usr\bin\bash.exe を指しているが、そちらは
# PATH に /usr/bin が入らないため node_modules/.bin のシェルシムが sed/dirname を
# 見つけられず tsc が起動しない。PATH を整えてくれる bin\bash.exe を使う。
script-shell=C:\\Program Files\\Git\\bin\\bash.exe
